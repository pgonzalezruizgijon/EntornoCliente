import { AbreviaturaPipe } from './abreviatura.pipe';

describe('AbreviaturaPipe', () => {
  it('create an instance', () => {
    const pipe = new AbreviaturaPipe();
    expect(pipe).toBeTruthy();
  });
});
